name: epitech
description: Oros Project.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  animate_do: ^3.0.2
  awesome_icons: ^1.1.0
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  http: ^1.2.2
  intl: ^0.19.0
  url_strategy: ^0.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
